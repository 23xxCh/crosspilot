# Issue Tracker

Issues 在 GitHub Issues 管理：`https://github.com/23xxCh/crosspilot`

## 使用方式

AI 工程技能（如 `to-issues`、`triage`、`to-prd`、`qa`）通过 `gh` CLI 操作：

```bash
gh issue list    # 列出 issue
gh issue create  # 创建 issue
gh issue view    # 查看详情
```

## 要求

- `gh` CLI 已安装并登录
- 仓库权限：read/write issues
