# 服务器部署包导航

服务器上传使用独立目录：`E:\AI WORK\服务器部署包\Amazon处理器_服务器版`。

该目录只包含 Windows 全天自动处理所需的核心代码、统一配置、Prompt、服务器后台脚本、输入收件箱、正式输出和排障文档。当前工作区保留完整测试、历史结果、本地模型和 `hfsy_*` 实验素材，方便继续开发，但不要整体复制到服务器。

服务器包不包含 `.env`、缓存、历史图片、Agnes 生图测试台和本地审图模型。首次部署时复制 `.env.example` 为 `.env`，在服务器本机填写密钥，再运行 `00_常用入口\04_一键安装服务器.bat`。
